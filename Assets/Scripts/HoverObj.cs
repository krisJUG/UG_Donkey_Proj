using UnityEngine;

/// <summary>
/// TODO
/// Provides hover and unhover for game object
/// </summary>
public class HoverObj : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
